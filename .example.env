# MCP Server env (e.g. when run by Claude Desktop). Copy to .env or set in editor MCP config.

# Project root containing .quikim/ (required for get_workflow_instruction and artifact paths)
QUIKIM_PROJECT_DIR=/path/to/your/project

# Project/API service base URL (defaults to project service in local mode)
QUIKIM_API_BASE_URL=http://localhost:8002

# Workflow service URL (required for get_workflow_instruction / report_workflow_progress)
QUIKIM_WORKFLOW_SERVICE_URL=http://localhost:8004

# Auth token (from quikim login or dashboard)
QUIKIM_API_KEY=

# Suppress MCP debug logs (set to 1 when using Claude Desktop)
QUIKIM_MCP_SILENT=1
